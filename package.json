{
  "name": "boardgame-online-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "functions"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "deploy": "npm run build && firebase deploy --only auth && node scripts/configure-auth.cjs && firebase deploy --only functions,database,hosting",
    "dev:emulator": "npm run dev --workspace client -- --mode emulator",
    "build": "npm run build --workspace functions && npm run build --workspace client",
    "lint": "npm run lint --workspace client && npm run lint --workspace functions",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "emulators": "npm run build --workspace functions && firebase emulators:start --project demo-boardgame --only auth,database,functions",
    "test:services": "node tests/auth-integration.mjs && node tests/integration.mjs && node tests/timebomb-integration.mjs && node tests/decorum-integration.mjs && node tests/splendor-integration.mjs && node tests/mafia-integration.mjs",
    "test:integration": "npm run build --workspace functions && firebase emulators:exec --project demo-boardgame --only auth,database,functions \"npm run test:services\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tonyleesos/boardgame-online-platform.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tonyleesos/boardgame-online-platform/issues"
  },
  "homepage": "https://github.com/tonyleesos/boardgame-online-platform#readme",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "firebase-tools": "^15.29.0",
    "prettier": "^3.9.6",
    "vitest": "^5.0.0"
  }
}
